  <div class="nav">
    <div class="wrapper-full">
      <nav>
        <ul class="nav-list">
          <li>
            <%= link_to wellspring.root_path, class: 'nav-item', 'aria-selected' => false do %>
              <i class="ion-ios-speedometer-outline"></i>
              Dashboard
            <% end %>
          </li>

          <li class="dropdown" data-behavior="dropdown">
            <%= link_to '#', class: 'nav-item', 'aria-selected' => false do %>
              <i class="ion-ios-photos-outline"></i>
              Entries
            <% end %>

            <ul>
              <% Wellspring.configuration.content_classes.each do |class_name| %>
                <li>
                  <%= link_to class_name.tableize.titleize, entries_path(content_class: class_name.tableize) %>
                </li>
              <% end %>
            </ul>
          </li>

          <li>
            <%= link_to wellspring.images_path, class: 'nav-item', 'aria-selected' => false do %>
              <i class="ion-ios-film-outline"></i>
              Images
            <% end %>
          </li>

          <li class="dropdown right" data-behavior="dropdown">
            <%= link_to "#", class: 'nav-item', 'aria-selected' => false do %>
              <i class="ion-ios-person"></i>
              <%= current_user.name %>
            <% end %>

            <ul>
              <li>
                <%= link_to "Sign out", signout_path %>
              </li>
            </ul>
          </li>
        </ul>
      </nav>
    </div>
  </div>
